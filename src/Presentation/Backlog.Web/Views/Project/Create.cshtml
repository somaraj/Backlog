@model ProjectModel
@{
    ViewData["Title"] = Localize("ProjectPage.Title");
}

<div class="card">
    <div class="card-body p-2">
        <form asp-action="Create">
            @await Html.PartialAsync("_Form", Model)
        </form>
    </div>
</div>